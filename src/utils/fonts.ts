import { Suez_One, Poppins } from "next/font/google";

export const suez = Suez_One({ style: "normal", weight: "400", subsets: ["latin"] });

export const poppins = Poppins({ style: "normal", weight: "400", subsets: ["latin"] });
